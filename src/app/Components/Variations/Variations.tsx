import {FenNode, GameTree} from "@/app/Components/ChessboardAnalysis/GameTree";

type VariationsProps = {
    gameTree: GameTree
};

function bracketOut(fenNode : FenNode) {
    if (fenNode.children.length )
}

export const Variations: React.FC<VariationsProps> = ({ gameTree }) => {
}
